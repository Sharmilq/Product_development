<?xml version="1.0" encoding="utf-8"?>
<!-- assessment_screen.dart exact replica: white bg, AppBar, progress bar, emoji+question, options, warning, back+next buttons -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- AppBar: "Health Assessment" Inter SemiBold 16sp, back arrow, white bg -->
    <androidx.appcompat.widget.Toolbar android:id="@+id/toolbar"
        android:layout_width="0dp" android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorSurface"
        app:title="Health Assessment"
        app:titleTextColor="?attr/colorOnSurface"
        app:navigationIcon="@drawable/ic_arrow_back_ios"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:navigationIconTint="?attr/colorOnSurface"/>

    <!-- Progress: "Question X of 13" + "X%" + thin progress bar (5dp, #E0E8EF track, #00BCD4 active) -->
    <LinearLayout android:id="@+id/layoutProgress"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp" android:paddingEnd="20dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView android:id="@+id/tvQuestionCount"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:fontFamily="@font/inter_regular"
                android:text="Question 1 of 13"
                android:textColor="?attr/colorOnSurfaceVariant" android:textSize="12sp"/>
            <TextView android:id="@+id/tvProgressPercent"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:fontFamily="@font/inter_regular"
                android:text="8%"
                android:textColor="?attr/colorOnSurfaceVariant" android:textSize="12sp"/>
        </LinearLayout>
        <Space android:layout_width="0dp" android:layout_height="6dp"/>
        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/progressBar"
            android:layout_width="match_parent" android:layout_height="5dp"
            app:indicatorColor="#00BCD4"
            app:trackColor="#E0E8EF"
            app:trackCornerRadius="4dp"/>
        <Space android:layout_width="0dp" android:layout_height="10dp"/>
    </LinearLayout>

    <!-- Scrollable content: emoji + question text + options -->
    <ScrollView android:id="@+id/scrollContent"
        android:layout_width="0dp" android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/layoutProgress"
        app:layout_constraintBottom_toTopOf="@id/layoutWarning"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="20dp" android:paddingEnd="20dp" android:paddingTop="24dp">

            <!-- Emoji 40sp -->
            <TextView android:id="@+id/tvEmoji"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:textSize="40sp"/>

            <Space android:layout_width="0dp" android:layout_height="16dp"/>

            <!-- Question text Inter Bold 20sp #1A2332, lineHeight=1.3 -->
            <TextView android:id="@+id/tvQuestion"
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:fontFamily="@font/inter_bold"
                android:textColor="?attr/colorOnSurface" android:textSize="20sp"
                android:lineSpacingMultiplier="1.3"/>

            <Space android:layout_width="0dp" android:layout_height="24dp"/>

            <!-- Options container — Java adds GestureDetector option rows dynamically -->
            <LinearLayout android:id="@+id/llOptions"
                android:layout_width="match_parent" android:layout_height="wrap_content"
                android:orientation="vertical"/>

        </LinearLayout>
    </ScrollView>

    <!-- Warning banner: bg=#FFF9E6 border=#FFE082 60%, radius=12dp, hidden by default -->
    <LinearLayout android:id="@+id/layoutWarning"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_marginStart="20dp" android:layout_marginEnd="20dp"
        android:layout_marginBottom="4dp"
        android:background="@drawable/bg_warning_banner"
        android:gravity="center_vertical" android:orientation="horizontal"
        android:padding="14dp" android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/layoutNavButtons"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        <ImageView android:layout_width="20dp" android:layout_height="20dp"
            android:src="@drawable/ic_warning_amber" />
        <Space android:layout_width="10dp" android:layout_height="0dp"/>
        <TextView android:id="@+id/tvWarning"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/inter_regular"
            android:textColor="#7B4A00" android:textSize="13sp"
            android:lineSpacingMultiplier="1.4"/>
        <ImageView android:id="@+id/btnDismissWarning"
            android:layout_width="16dp" android:layout_height="16dp"
            android:src="@drawable/ic_close" />
    </LinearLayout>

    <!-- Back + Next buttons row -->
    <LinearLayout android:id="@+id/layoutNavButtons"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_marginStart="20dp" android:layout_marginEnd="20dp"
        android:layout_marginBottom="16dp"
        android:gravity="center_vertical" android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Back button (hidden on Q1): outlined, padding=14dp, radius=12dp, #E0E8EF border -->
        <com.google.android.material.button.MaterialButton android:id="@+id/btnBack"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:visibility="gone"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:text="← Back" android:textColor="?attr/colorOnSurfaceVariant"
            android:fontFamily="@font/inter_medium"
            android:paddingTop="14dp" android:paddingBottom="14dp"
            app:strokeColor="#E0E8EF" app:cornerRadius="12dp"/>

        <Space android:id="@+id/spacerBetweenButtons"
            android:layout_width="12dp" android:layout_height="0dp" android:visibility="gone"/>

        <!-- Next / See Results: disabled when no answer selected -->
        <com.google.android.material.button.MaterialButton android:id="@+id/btnNext"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="2"
            android:text="Next →" android:textColor="?attr/colorOnSurface"
            android:fontFamily="@font/inter_semibold"
            android:paddingTop="14dp" android:paddingBottom="14dp"
            app:backgroundTint="#00BCD4" app:cornerRadius="12dp" app:elevation="0dp"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
