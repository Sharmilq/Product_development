<?xml version="1.0" encoding="utf-8"?>
<!--
  Single assessment option row
  Unselected: white bg, #E0E8EF border 1dp, text #1A2332 Inter Regular 14sp
  Selected: #00BCD4 08% bg, #00BCD4 border 1.5dp, text #00BCD4 Inter SemiBold 14sp
  Right: 20dp radio circle (empty or filled cyan with white check)
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_option_unselected"
    android:paddingStart="16dp" android:paddingEnd="16dp"
    android:paddingTop="14dp" android:paddingBottom="14dp">

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:gravity="center_vertical" android:orientation="horizontal">

        <TextView android:id="@+id/tvOptionText"
            android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/inter_regular"
            android:textColor="#1A2332" android:textSize="14sp"/>

        <!-- Radio circle: 20dp, empty = transparent bg + #D0D8E4 border 2dp -->
        <FrameLayout android:id="@+id/radioCircle"
            android:layout_width="20dp" android:layout_height="20dp"
            android:background="@drawable/bg_radio_unselected">
            <ImageView android:id="@+id/ivCheck"
                android:layout_width="12dp" android:layout_height="12dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_check_white"
                android:visibility="gone"/>
        </FrameLayout>

    </LinearLayout>
</FrameLayout>
