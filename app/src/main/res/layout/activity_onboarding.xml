<?xml version="1.0" encoding="utf-8"?>
<!-- onboarding_screen.dart: bg=#DFF5F9, skip top-right, ViewPager2, dots, Next/GetStarted button -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="#DFF5F9">

    <TextView android:id="@+id/tvSkip"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_marginTop="12dp" android:layout_marginEnd="20dp"
        android:padding="8dp" android:text="Skip"
        android:fontFamily="@font/inter_regular"
        android:textColor="?attr/colorOnSurfaceVariant" android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <androidx.viewpager2.widget.ViewPager2 android:id="@+id/viewPager"
        android:layout_width="0dp" android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/tvSkip"
        app:layout_constraintBottom_toTopOf="@id/llDots"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Dots: animated via Java (active=20dp wide #00BCD4, inactive=8dp #FFFFFF) -->
    <LinearLayout android:id="@+id/llDots"
        android:layout_width="wrap_content" android:layout_height="8dp"
        android:orientation="horizontal" android:gravity="center_vertical"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toTopOf="@id/btnNext"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <com.google.android.material.button.MaterialButton android:id="@+id/btnNext"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_marginStart="24dp" android:layout_marginEnd="24dp"
        android:layout_marginBottom="32dp"
        android:text="Next" android:textColor="?attr/colorOnSurface" android:textSize="15sp"
        android:fontFamily="@font/inter_semibold"
        android:paddingTop="14dp" android:paddingBottom="14dp"
        app:backgroundTint="#00BCD4" app:cornerRadius="12dp" app:elevation="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
